`similarity and dissimilarity tell us how close or how different two data objects are`

## Similarity
It means how much two objects are alike 
Range from 0 to 1 

## Dissimilarity

It means how much two objects are different
Often represented using distance measures
• 0 = exactly same
• Larger value = more different 
